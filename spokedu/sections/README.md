# sections/

SSI 또는 빌드 툴로 `index.html`에 include할 때 사용하는 HTML 조각 폴더입니다.

현재는 `index.html`이 단일 파일에 본문을 모두 포함하고 있으며,  
새 섹션을 추가할 때 이 폴더에 파일을 만든 뒤 `index.html`에 삽입하는 방식으로 확장할 수 있습니다.

가이드 참고: 프로젝트 루트의 **SPOKEDU 랜딩 페이지 — 파일 구조 가이드**.
