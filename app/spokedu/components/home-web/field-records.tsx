import Link from 'next/link';
import { HOME_FIELD_RECORDS } from './home-web-assets';
import styles from './home-web.module.css';
function RecordImage({record,className}:{record:(typeof HOME_FIELD_RECORDS)[number];className:string}) { return <Link href={record.href} className={`${className} ${styles.recordTile}`} aria-label={`${record.title} — ${record.place} 사례 보기`}><figure><img src={record.src} alt={`${record.place} ${record.title} 수업 현장`} /><figcaption><strong>{record.title}</strong><span>{record.place}</span><b aria-hidden="true">↗</b></figcaption></figure></Link>; }
export function FieldRecords() { return <section className={`${styles.section} ${styles.records}`} aria-labelledby="home-records-heading"><div className={styles.recordsTop}><div><p className={styles.kicker}>FIELD RECORDS</p><h2 id="home-records-heading" className={styles.title}>수업은<br />현장에서 증명됩니다.</h2></div><p className={styles.body}>학교, 기관, 지역사회와 함께한<br />다양한 수업 사례를 통해 SPOKEDU의 가치를 확인할 수 있습니다.</p></div><div className={styles.recordCollage}><RecordImage record={HOME_FIELD_RECORDS[0]} className={styles.recordLarge}/><div className={styles.recordRight}><RecordImage record={HOME_FIELD_RECORDS[1]} className={styles.recordSmall}/><RecordImage record={HOME_FIELD_RECORDS[2]} className={styles.recordSmall}/></div></div></section>; }