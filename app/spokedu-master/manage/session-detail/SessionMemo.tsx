export function SessionMemo({ isCreate, memo, onChange }: { isCreate: boolean; memo: string; onChange: (memo: string) => void }) {
  return <section className={isCreate ? 'mt-5' : 'mt-5 border-t border-slate-100 pt-4'}><div className="flex min-h-10 items-center justify-between"><span className="text-[18px] font-semibold text-slate-950">메모</span><span className="text-xs font-normal text-slate-400">선택사항</span></div><textarea value={memo} onChange={(event) => onChange(event.target.value)} placeholder="수업에 대한 메모를 남겨보세요." className={`${isCreate ? 'mt-2 h-16 min-h-[60px] max-h-[68px] resize-y' : 'mt-1 min-h-[72px]'} w-full rounded-[12px] border border-slate-200 p-3 text-sm text-slate-800 outline-none focus:border-slate-300 focus:ring-2 focus:ring-blue-500/20`} />{isCreate ? null : <p className="mt-1 text-right text-xs text-slate-400">{memo.length} / 500</p>}</section>;
}
