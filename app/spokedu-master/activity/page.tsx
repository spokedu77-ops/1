import ManageView from '../manage/ManageView';

/** Compatibility route for existing Session, Library, SPOMOVE, and record deep-links. */
export default function ActivityCompatibilityPage() {
  return <ManageView />;
}
